output {
  if [type] == "syslog" {
    elasticsearch {
        hosts => [ "elasticsearch-es-http.default.svc.cluster.local" ]
        user => "elastic"
        password => "Xc4b1540j44E67wYcud1lOWX"
        ssl => true
        cacert => "/usr/share/logstash/config/ca.crt"
        ssl_certificate_verification => false
        index => "logstash-pfsense-%{+YYYY.MM.dd}"
    }
  }
}
